# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - artwork-service-container

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://yanayartwork.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_74ff4344a5804b408c736c243373d98f }}
        password: ${{ secrets.AzureAppService_ContainerPassword_ffab61b2e72649cfb6fb92ea04d48cfd }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: yanayartwork.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_74ff4344a5804b408c736c243373d98f }}/artwork-service:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'artwork-service-container'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_7597b950b8f5474c852299162746189e }}
        images: 'yanayartwork.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_74ff4344a5804b408c736c243373d98f }}/artwork-service:${{ github.sha }}'